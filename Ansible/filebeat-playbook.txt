---
- name: Installation  and launch filebeat
  hosts: webservers
  become: yes
  tasks:

   #download filebeat file using command
  - name: Download filebeat .deb file
    command: curl -L -O https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-7.4.0-amd64.deb

   #install package Using command
  - name: install filebeat.deb
    command: dpkg -i filebeat-7.4.0-amd64.deb

   #copy files
  - name: Drop in filebeat.yml
    copy:
      src: /etc/ansible/files/filebeat-config.yml
      dest: /etc/filebeat/filebeat.yml

   #enable filebeat module using command
  - name: Enable and configure system module
    command: filebeat modules enable system

  #Setup filebeat Using command
  - name: Setup filebeat
    command: filebeat setup

  #start filebeat using command
  - name: Start filebeat service
    command: service filebeat start

  #enable filebeat on system boot
  - name: enable service filebeat on boot
    systemd:
      name: filebeat
      enabled: yes